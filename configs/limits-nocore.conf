# vps-max-security — disable core dumps system-wide.
# Core dumps routinely leak secrets (JWT keys, DB passwords, env vars) from
# Django/Node process memory. Paired with fs.suid_dumpable=0 in sysctl.
* hard core 0
* soft core 0
root hard core 0
root soft core 0
