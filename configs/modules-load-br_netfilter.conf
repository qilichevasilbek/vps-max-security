# vps-max-security — ensure br_netfilter loads at boot
# Required for net.bridge.bridge-nf-call-iptables sysctl keys.
# Without this, /etc/sysctl.d/99-hardening.conf fails to apply on first boot.
br_netfilter
